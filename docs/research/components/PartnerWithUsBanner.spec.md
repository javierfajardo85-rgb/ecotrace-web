# PartnerWithUsBanner Specification (Solugen-inspired)

## Overview
- **Target file:** `src/components/PartnerWithUsBanner.tsx`
- **Placement:** Below `GreenNotaryProtocol` on the home page (`src/app/page.tsx`)
- **Interaction model:** hover-driven button

## Visual intent
- A large, rounded rectangle “panel” with a moving GIF/video background (placeholder for now).
- Centered quote text (verbatim).
- A pill CTA button centered below with a circular image slot on the left (blank placeholder for now).

## Copy (verbatim)
Quote:
`We’re committed to solving complex challenges by working side by side with our customers to create lasting impact. Together, let’s tackle what’s next.`

Button:
`Partner With Us`

## Layout
- Panel: full width within max container, large vertical padding, rounded corners, overflow hidden.
- Background placeholder layer: gradient/noise to stand in for the future GIF.
- Quote centered, max width ~900px.
- CTA button centered; circular avatar slot left of text.

