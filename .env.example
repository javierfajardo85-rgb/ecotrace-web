RESEND_API_KEY=your_resend_api_key
CONTACT_FROM_EMAIL="EcoTrace Contact <onboarding@resend.dev>"

# Customer dashboard password gate (/dashboard/*). Set these in the hosting env.
# Without DASHBOARD_PASSWORD the dashboard returns 503 (locked).
DASHBOARD_USER=ecotrace
DASHBOARD_PASSWORD=change-me
